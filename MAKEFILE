CC=g++ #change to gcc if on windows potentially
FLAGS=-Wall -g
LIBS =
RULE=MEM50
RM=rm
BUILD_L = ./build.log
OUTPUT = ./output.log

all:
	$(CC) $(FLAGS) ./src/*.cpp -o rule_$(RULE) $(LIBS) 2>> $(BUILD_L)

clean:
	$(RM) $(OUT) $(BUILD_L)
